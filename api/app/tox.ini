[tox]
envlist = test,flake8,isort
skipsdist = True

[testenv]
envdir = {toxworkdir}/test
deps = -r../requirements-root.txt
basepython = python3
commands =
    test: pytest {posargs:} --tb=short
    flake8: flake8
    isort: isort --recursive --check-only .

[pytest]
DJANGO_SETTINGS_MODULE = signals.settings

[flake8]
max-complexity = 8
max-length = 100

[isort]
skip = .tox
not_skip = __init__.py
multi_line_output = 4
